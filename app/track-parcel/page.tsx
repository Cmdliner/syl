export default function TrackParcel() {
    return (
        <div>
            {/* Search bar for input of tracking id */}

            {/* container for animation or image when there is no result or when no data to search */}

            {/* map that shows riders location when rider is found */}
        </div>
    );
}